# This file is managed by Chef!
port=0
enable-tftp
tftp-root=<%= @tftproot %>
dhcp-boot=pxelinux.0
bind-interfaces
dhcp-range=<%= @dhcp_range %>
<% @static_dhcp_hosts.each do |host| %>
dhcp-host=<%= host %>
<% end %>
